[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "java-stubs"
version = "25.1.3"
description = "Type stubs for the GraalPy java module"
requires-python = ">=3.12,<3.15"

[tool.hatch.build.targets.wheel]
packages = ["java"]
